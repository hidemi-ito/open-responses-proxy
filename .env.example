# =============================================================================
# Open Responses API — Environment Variables
# =============================================================================

# Authentication
# Comma-separated list of valid Bearer tokens. Leave empty to allow all tokens.
API_KEYS=

# Anthropic (Claude)
ANTHROPIC_API_KEY=sk-ant-xxx

# gpt-oss (OpenAI-compatible endpoint, e.g. vLLM / Ollama)
GPT_OSS_BASE_URL=http://localhost:8000/v1
GPT_OSS_API_KEY=

# PostgreSQL (Drizzle ORM)
DATABASE_URL=postgres://user:pass@localhost:5432/responses

# S3-compatible blob storage (file uploads)
S3_ENDPOINT=https://s3.amazonaws.com
S3_BUCKET=responses-files
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_REGION=us-east-1
